# Shallow research only (CLI or API).
# It has the following features:
# - No intent classifier, clarifier, or deep research agent
# - Web search tools only (same Tavily settings as config_cli_default.yml)
# - There is no knowledge retrieval

general:
  telemetry:
    logging:
      console:
        _type: console
        level: INFO

  front_end:
    _type: aiq_api
    runner_class: aiq_api.plugin.AIQAPIWorker
    db_url: ${NAT_JOB_STORE_DB_URL:-sqlite+aiosqlite:///./jobs.db}
    expiry_seconds: 86400  # 24 hours (min: 600, max: 604800/7 days)
    dask_workers: threads
    max_running_async_jobs: 1
    cors:
      allow_origin_regex: 'http://localhost(:\d+)?|http://127.0.0.1(:\d+)?'
      allow_methods:
        - GET
        - POST
        - DELETE
        - OPTIONS
      allow_headers:
        - "*"
      allow_credentials: true
      expose_headers:
        - "*"

llms:
  # Known limitation: Build-hosted Lightning can intermittently produce citation-incomplete shallow drafts.
  # AI-Q fails closed; see docs/source/resources/troubleshooting.md#nemotron-35-lightning-on-nvidia-api-catalog.
  nemotron_lightning_agent_llm:
    _type: nim
    model_name: nvidia/nemotron-3.5-lightning-30b-a3b
    base_url: "https://integrate.api.nvidia.com/v1"
    api_key: ${NVIDIA_API_KEY}
    temperature: 0.2
    top_p: 0.7
    max_tokens: 32768
    num_retries: 5
    parallel_tool_calls: false
    chat_template_kwargs:
      enable_thinking: true

functions:
  # =========================================================================
  # Data Source Registry
  # =========================================================================
  # Central registry that controls:
  #   1. UI toggles — each source appears as an on/off switch in the frontend
  #   2. Per-message filtering — users can select active sources per request
  #   3. Tool auto-inheritance — agents with no explicit `tools` list receive
  #      every tool listed here (use `exclude_tools` on agents to specialize)
  #
  # Source entry fields:
  #   id, name, description, tools, requires_auth (default: false),
  #   default_enabled (default: true)
  #
  # See docs/source/customization/tools-and-sources.md for full details.
  # =========================================================================
  data_sources:
    _type: data_source_registry
    sources:
      - id: web_search
        name: "Web Search"
        description: "Search the web for real-time information."
        tools:
          - web_search_tool
          - advanced_web_search_tool
      # - id: paper_search
      #   name: "Academic Papers"
      #   description: "Search academic papers and scientific publications."
      #   tools:
      #     - paper_search_tool

  web_search_tool:
    _type: tavily_web_search
    max_results: 5
    max_content_length: 1000

  advanced_web_search_tool:
    _type: tavily_web_search
    max_results: 2
    advanced_search: true

  # Paper Search (optional - requires SERPER_API_KEY)
  # Uncomment the block below and set SERPER_API_KEY to enable academic paper search.
  # paper_search_tool:
  #   _type: paper_search
  #   max_results: 5
  #   serper_api_key: ${SERPER_API_KEY}

  # =========================================================================
  # Agents — inherit all registry tools; use exclude_tools to specialize
  # =========================================================================
  shallow_research_agent:
    _type: shallow_research_agent
    llm: nemotron_lightning_agent_llm
    # tools: omitted -> inherits all from data_source_registry
    exclude_tools:
      - advanced_web_search_tool
    max_llm_turns: 10
    max_tool_iterations: 5

workflow:
  _type: shallow_research_workflow
  # Uncomment to export Relay traces to a local Phoenix instance.
  # relay:
  #   observability:
  #     opentelemetry:
  #       enabled: true
  #       endpoints:
  #         - type: openinference
  #           endpoint: ${RELAY_OTEL_ENDPOINT:-http://localhost:6006/v1/traces}
  #           service_name: aiq-relay
  #           resource_attributes:
  #             openinference.project.name: aiq-relay
  #             deployment.environment: development
